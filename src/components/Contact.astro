<section>
    <div
        class="flex flex-col justify-center items-center p-8 shadow-2xl rounded-2xl"
    >
        <h3 class="font-serif">Contáctanos</h3>
        <p>Déjanos tu consulta y nos comunicaremos a la mayor brevedad</p>

        <form action="" class="flex flex-col gap-4 max-w-lg">
            <div class="flex flex-col gap-1 w-full">
                <label for="name">Nombre</label>
                <input
                    type="text"
                    name="Nombre"
                    id="name"
                    class="bg-gray-200 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-orange-600"
                />
            </div>
            <div class="flex flex-col gap-1">
                <label for="name">Nombre</label>
                <input
                    type="email"
                    name="Nombre"
                    id="name"
                    class="bg-gray-200 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-orange-600"
                />
            </div>
            <textarea
                name="message"
                id="message"
                class="bg-gray-200 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-orange-600"
            >
            </textarea>
            <button
                type="submit"
                class="bg-black text-white px-6 py-3 rounded-full"
                >Enviar</button
            >
        </form>
    </div>
</section>
